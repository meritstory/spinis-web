{% extends 'base.html.twig' %}

{% block title %}{{ 'faq_page.title'|trans }}{% endblock %}

{% block body %}
    <main>
        <section class="pt-8 pb-16 bg-page">
            <div class="mx-auto max-w-[1200px] px-6">
                {% include '_breadcrumb.html.twig' with {crumbs: [{label: 'faq_page.breadcrumb.current'|trans}]} %}

                <h1 class="mt-8 mb-5 text-heading-h1 font-bold">{{ 'faq_page.heading'|trans }}</h1>
                <p class="max-w-[720px] text-body-lg text-text-secondary">{{ 'faq_page.description'|trans }}</p>
            </div>
        </section>

        <section class="py-20 bg-white">
            <div class="mx-auto max-w-[1200px] px-6">
                <div class="border-t border-border-default">
                    {% for faq in faqs %}
                        <details name="faq-accordion" {% if loop.first %}open{% endif %} class="group border-b border-border-default py-8">
                            <summary class="flex list-none items-center justify-between gap-4 text-[15px] font-semibold [&::-webkit-details-marker]:hidden">
                                {{ faq.question }}
                                <img src="{{ asset('images/chevron.svg') }}" class="h-6 w-6 shrink-0 transition-transform duration-150 group-open:rotate-180" alt="">
                            </summary>
                            <div class="mt-8 text-body-md text-gray-600">{{ faq.answer|sanitize_html }}</div>
                        </details>
                    {% endfor %}
                </div>
            </div>
        </section>

        {% include '_cta_section.html.twig' with {
            heading: 'faq_page.still_questions.heading'|trans,
            description: 'faq_page.still_questions.description'|trans,
            cta: 'faq_page.still_questions.cta'|trans,
        } %}
    </main>
{% endblock %}
