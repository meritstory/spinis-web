<div class="ea-complaint-field">
    <div class="ea-complaint-field__label">{{ label }}</div>
    <div class="ea-complaint-field__value">
        {% if raw|default(false) %}{{ value|raw }}{% else %}{{ value }}{% endif %}
    </div>
</div>
