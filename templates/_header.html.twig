<header class="site-header">
    <div class="container site-header__inner">
        <a href="{{ path('home') }}" class="site-header__logo">
            <span class="site-header__logo-accent">SKUNDŲ</span> NAGRINĖJIMO SISTEMA
        </a>

        <nav class="site-header__nav">
            <a href="#">Apie sistemą</a>
            <a href="#">D.U.K.</a>
            <a href="#">Privatumo politika</a>
            <a href="#">Kontaktai</a>
        </nav>

        <div class="user-menu" data-controller="dropdown">
            <button
                type="button"
                class="user-menu__toggle"
                data-action="dropdown#toggle click@window->dropdown#closeOnClickOutside"
                data-dropdown-target="toggle"
                aria-haspopup="true"
                aria-expanded="false"
                aria-label="Vartotojo meniu"
            >
                <svg viewBox="0 0 24 24" fill="none" width="20" height="20">
                    <circle cx="12" cy="8" r="4" stroke="currentColor" stroke-width="1.5"/>
                    <path d="M4 20c0-4.418 3.582-7 8-7s8 2.582 8 7" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
                </svg>
            </button>

            <div class="user-menu__panel" data-dropdown-target="panel" hidden>
                <a href="#" class="user-menu__item">Prisijungti</a>
            </div>
        </div>
    </div>
</header>
